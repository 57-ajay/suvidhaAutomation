[project]
name = "border-tax-worker"
version = "1.0.0"
description = "Border-tax worker: scripted CheckPost runner driven over CDP"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.12,<0.13",
    "redis>=5.0",
    "httpx>=0.27",
    "pydantic>=2.7",
    "google-genai>=1.20",
]

[tool.uv]
package = false
