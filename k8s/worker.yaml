apiVersion: apps/v1
kind: Deployment
metadata: { name: worker }
spec:
  replicas: 1
  selector: { matchLabels: { app: worker } }
  template:
    metadata:
      labels: { app: worker }
      annotations:
        cluster-autoscaler.kubernetes.io/safe-to-evict: "false"
    spec:
      serviceAccountName: worker-sa
      nodeSelector: { role: worker }
      tolerations:
        - { key: dedicated, operator: Equal, value: worker, effect: NoSchedule }
      terminationGracePeriodSeconds: 3600
      containers:
        - name: worker
          image: asia-south1-docker.pkg.dev/cabswale-ai/suvidha/worker:v8
          ports: [{ containerPort: 6080 }]
          envFrom: [{ secretRef: { name: suvidha-env } }]
          env:
            - { name: REDIS_URL, value: "redis://redis:6379" }
            - { name: API_URL, value: "http://api:3000" }
            - { name: MAX_SLOTS, value: "12" }
            - { name: DOMAIN, value: "gkep.cabswale.in" }
            - {
                name: POD_IP,
                valueFrom: { fieldRef: { fieldPath: status.podIP } },
              }
          volumeMounts: [{ name: dshm, mountPath: /dev/shm }]
          resources:
            requests: { cpu: "3", memory: 12Gi }
            limits: { memory: 13Gi }
      volumes:
        - name: dshm
          emptyDir: { medium: Memory, sizeLimit: 2Gi }
